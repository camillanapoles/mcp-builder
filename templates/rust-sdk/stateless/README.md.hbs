# {{name}}

> MCP server generated by **mcp-builder** (Rust SDK / stateless pattern)

{{description}}

## Architecture — Stateless Execution Pattern

Pure functions, no shared state, deterministic. Built with
[rmcp](https://github.com/modelcontextprotocol/rust-sdk).

## Build & Run

```bash
cargo build --release
./target/release/{{name}}
```

## Test

```bash
cargo test --verbose
```

## Tools

{{#each tools}}
### `{{name}}`
{{description}}
{{/each}}

{{#unless tools}}
### `hello` (default)
Default greeting tool.
{{/unless}}

## License

{{#if metadata.license}}{{metadata.license}}{{else}}MIT{{/if}}
