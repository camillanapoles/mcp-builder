# Worklog — mcp-builder project

---
Task ID: ALL (single-agent execution)
Agent: main (super-z)
Task: Build complete MCP Builder blueprint with templates, workflows, hooks, tests, and example project

Work Log:
- Asked 8 clarifying questions to user; received detailed answers (TS+Py hybrid, 4 SDK targets, 4 invocation modes, GitHub Actions FSM, hybrid repo+cache, all 5 scopes, all 5 test layers, MD docs, hooks system as plugin for Claude/Codex)
- Created monorepo structure at /home/z/my-project/mcp-builder/
- Wrote BLUEPRINT.md (12 sections, ~600 lines) covering: contexto, arquitetura, FSM GitOps, hooks system, padrões de design, builder, testes, estrutura monorepo, PDCA loop, compliance, roadmap, referências
- Wrote 4 ADRs (0001-hybrid-ts-py, 0002-gitops-as-fsm, 0003-hooks-bridge, 0004-five-test-layers)
- Created FSM YAML files (.mcp/state/states.yaml + transitions.yaml) with 7 states, 7 transitions, wildcard failure recovery
- Implemented Builder core TS in builder/src/:
  - types.ts (shared types: MCPSpec, FSMContext, Hook, Artifact, etc.)
  - fsm/index.ts (FSM engine with fromRepo, transition, apply, toMermaid)
  - validator/index.ts (AJV + JSON Schema, validateMCPSpec, validateToolSpec)
  - logger/index.ts (pino wrapper)
  - scaffolder/index.ts (Handlebars render with custom helpers: pascalCase, camelCase, snakeCase, kebabCase, json)
  - hooks/registry.ts (HookRegistry with runCategory, short-circuit on block)
  - hooks/gate.ts (validateSpec, validateTemplate, checkPrereqs, checkQuota)
  - hooks/trigger.ts (dispatchAction, dispatchLocal, openPR, notifyWebhook)
  - hooks/event.ts (EventBus pub/sub, routeEvent, subscribeEvent, CANONICAL_EVENTS)
  - hooks/monitor.ts (collectMetrics, validateDeterminism, hashProject, streamLogs)
  - hooks/advisor.ts (checkCoverage, checkMutation, checkCompliance, checkContracts, signOff, collectFailure)
  - cli/index.ts (commander: new, serve, serve:http, list-templates, validate, fsm-show, fsm-transition)
  - mcp/server.ts (MCP server próprio with 7 tools: create_project, add_tool, run_tests, validate_spec, transition_state, list_templates, fsm_show)
  - http/server.ts (Fastify REST: /generate, /validate, /templates, /fsm/mermaid, /fsm/transition, /health)
  - action/index.ts (GitHub Action adapter with @actions/core + @actions/github)
  - index.ts (public API exports)
- Created 4 SDK templates:
  - Python (stateless + event + factory) — 3 patterns, each with README, pyproject.toml, mcp.json, src/, tests/ (unit+property+contract), .mcp/state/, .github/workflows/, docs/adr/
  - TypeScript (stateless) — package.json, src/index.ts (using @modelcontextprotocol/sdk), tests (unit + property), FSM
  - Go (stateless) — go.mod, cmd/server/main.go (using mark3labs/mcp-go), internal/tools/, tests
  - Rust (stateless) — Cargo.toml, src/main.rs (using rmcp), tests
- Created 6 GitHub Actions workflows in .github/workflows/:
  - scaffold.yml (draft → scaffolded, with gate + advisor hooks, artifact upload, triggers test.yml)
  - test.yml (scaffolded → tested, matrix strategy for unit/contract/property suites, triggers e2e.yml)
  - e2e.yml (tested → validated, downloads artifact, spawns MCP server, computes determinism hash, triggers release.yml)
  - release.yml (validated → released, mutation testing gate ≥70%, creates git tag + GitHub Release)
  - advisor-block.yml (* → failed, opens issue with reason + suggestions, FSM state file)
  - reset.yml (failed → draft, triggered by /advisor-clear issue comment, validates permissions)
- Wrote test suites in builder/tests/:
  - fsm.test.ts (19 tests — basic queries, validTransitions, transition with wildcards, apply immutability, toMermaid)
  - validator.test.ts (15 tests — accept valid, reject invalid name/sdk/pattern/duplicates/etc)
  - hooks.test.ts (9 tests — register, run, runCategory with short-circuit, list)
  - property.test.ts (5 tests — fast-check invariants: initial state, history monotonic, terminal no transitions, immutability, mermaid format)
  - contract.test.ts (17 tests — templates have required files, FSM YAML valid, builder exports, happy path transitions)
  - stryker.config.json (mutation testing config, threshold 70%, mutates fsm/validator/hooks/scaffolder)
- Created example project examples/echo-stateless/ (Python, generated by builder, with unit + property + contract tests)
- Created documentation:
  - docs/BLUEPRINT.md (12 sections, Mermaid diagrams)
  - docs/GETTING_STARTED.md (6-step tutorial)
  - docs/HOOKS.md (complete hooks reference with examples)
  - docs/TEMPLATES.md (how to create new templates)
  - docs/adr/0001-0004 (architectural decisions)
  - hooks/README.md (plugin system for custom hooks)
  - README.md, CHANGELOG.md, LICENSE, .gitignore
- Created scripts/generate-example.sh
- Validated end-to-end:
  - TypeScript: npm install (623 packages), tsc compiles clean, vitest 65/65 tests pass
  - CLI: `mcp-builder new`, `fsm-show`, `fsm-transition`, `validate` all working
  - Generated 4 MCP projects (Python/TS/Go/Rust) successfully via CLI
  - Fixed bugs: template path resolution (3 levels up not 2), Handlebars escape for GitHub Actions `${{ }}`, snakeCase regex (was only replacing first hyphen), missing `json` helper

Stage Summary:
- Total files created: 80+
- Test coverage: 65/65 unit + property + contract tests passing
- 4 SDK templates × 3 patterns (Python has all 3, others have stateless)
- 6 GitHub Actions workflows implementing complete FSM lifecycle
- 5 hooks canônicos (gate/trigger/event/monitor/advisor) with 16+ built-in implementations
- 4 invocation modes (CLI/MCP/HTTP/Action) all implemented
- 4 ADRs documenting architectural decisions
- Builder core compiles clean (tsc strict mode), runs as CLI, generates valid projects
- Example echo-stateless project ready for `pip install -e . && pytest`
- Blueprint, ADRs, GETTING_STARTED, HOOKS, TEMPLATES docs all in Markdown versionados no repo
- All design decisions traceable to user's clarifying answers (TS+Py, 4 SDKs, 4 modes, Actions FSM, repo+cache, all scopes, all test layers, MD docs, hooks as agent plugins)

---
Task ID: v1.1.0-incremental
Agent: main (super-z)
Task: Continuidade incremental — checkpoint + adoção metodologia Blueprint + matriz 4×3 + hooks customizados + mutation testing + GitHub repo setup

Work Log:
- Checkpoint v1.0.0 criado: git init + commit + tag
- Leu 100% dos 2 blueprints anexos (blueprints.md + blueprint-reference.md, 44KB total)
- Identificou 5 seções canônicas para adotar: initialize, maintenance, knowledge, post-build, clone
- Criou módulo builder/src/blueprint/ (types.ts + parser.ts + executor.ts + index.ts)
- Adicionou 3 subcomandos CLI: blueprint-validate, blueprint-run, blueprint-knowledge
- 31 testes do blueprint (parser: 17, executor: 14) cobrindo multi-document, ENVRC propagation, post-build scope, secrets, hash
- Criou blueprint.yaml.hbs para os 12 templates + 1 raiz (.mcp/blueprint.yaml) + 1 exemplo (examples/echo-stateless/blueprint.yaml)
- Documentação BLUEPRINT-METHODOLOGY.md (12 seções, ~450 linhas) explicando adoção das práticas Devin sem usar Devin
- Testou exemplo echo-stateless: pip install + pytest → 38/38 passing, coverage 81.25% (acima do gate 80%)
- Adicionou test_server.py para cobrir resources.py + server.py (subiu de 31% para 81%)
- Preencheu matriz 4×3 (12 templates): TS event, TS factory, Go event, Go factory, Rust event, Rust factory (via script Python)
- Validou geração de todos os 12 templates via CLI real
- Criou 5 hooks customizados em hooks/:
  - cost-limit.ts (gate) — bloqueia se custo LLM > orçamento
  - audit-log.ts (monitor) — append-only audit log + webhook sink
  - dependency-vulns.ts (advisor) — bloqueia release com critical/high vulns
  - rate-limit.ts (gate) — sliding window rate limiter
  - prompt-injection-detector.ts (gate) — detecta 7 padrões (DAN, ignore previous, role override, etc)
- 27 testes cobrindo os 5 hooks
- Rodou stryker mutation testing no validator: 62.60% score (77 killed, 37 survived, 9 no coverage) — acima do break 50%
- Criou .github/PULL_REQUEST_TEMPLATE.md (checklist completo: tipo, escopo, testes, blueprint, FSM, hooks, ADRs)
- Criou .github/REPO-SETUP.md (branch protection, secrets, workflows, Dependabot, CODEOWNERS, Pages, releases)
- Criou .mcp/CLAUDE-CODE-SETUP.md (instruções para Claude Code / Cursor / Cline)
- Criou .mcp/claude-code-config.example.json (config pronta para copiar)
- Validação final: 123/123 builder tests + 38/38 example tests + 12/12 templates geram projetos + 12/12 blueprints validados
- Commit v1.1.0 + tag criados
- CHECKPOINT.md atualizado com métricas comparativas v1.0.0 vs v1.1.0 e roadmap incremental (v1.2.0, v1.3.0, v2.0.0)

Stage Summary:
- Tags git: v1.0.0, v1.1.0
- 206 arquivos no monorepo (+82 vs v1.0.0)
- 84 templates Handlebars (+33 vs v1.0.0)
- 123/123 builder tests passing (+58 vs v1.0.0)
- 38/38 example tests passing (coverage 81.25%)
- 5 hooks customizados com 27 testes
- Mutation score: 62.60% no validator (acima do break threshold 50%)
- 14 blueprint.yaml files (12 templates + raiz + exemplo)
- Metodologia Blueprint Devin-style completamente adotada e documentada, sem usar Devin
- GitHub repo setup pronto (PR template, REPO-SETUP, Claude Code config)
